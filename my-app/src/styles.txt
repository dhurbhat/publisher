      * {
          box-sizing: border-box;
          margin: 0;
          padding: 0;
      }

      body {
          font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
          color: #1f2937;
          background: #ffffff;
          overflow: hidden;
          height: 100vh;
      }

      .toolbar {
          height: 56px;
          border-bottom: 1px solid #e5e7eb;
          display: flex;
          align-items: center;
          padding: 0 16px;
          gap: 16px;
          background: #fafafa;
      }

      .toolbar button {
          padding: 8px 12px;
          background: #ffffff;
          border: 1px solid #d1d5db;
          border-radius: 6px;
          cursor: pointer;
          font-weight: 500;
      }

      .toolbar button:hover {
          background: #f3f4f6;
      }

      .novel-title {
          font-weight: 600;
          font-size: 1.1rem;
          margin-left: auto;
          margin-right: auto;
      }

      .workspace-container {
          display: flex;
          height: calc(100vh - 56px);
          position: relative;
      }

      .left-sidebar {
          width: 260px;
          border-right: 1px solid #e5e7eb;
          background: #f9fafb;
          overflow-y: auto;
          transition: transform 0.3s ease, margin-left 0.3s ease;
          padding: 24px 16px;
      }

      .left-sidebar.collapsed {
          margin-left: -260px;
      }

      .left-sidebar h3 {
          font-size: 0.85rem;
          text-transform: uppercase;
          color: #6b7280;
          margin-bottom: 12px;
      }

      .left-sidebar ul {
          list-style: none;
      }

      .left-sidebar li {
          margin-bottom: 8px;
      }

      .left-sidebar a {
          text-decoration: none;
          color: #4b5563;
          font-size: 0.95rem;
      }

      .main-reader {
          flex: 1;
          overflow-y: auto;
          padding: 40px 60px;
          display: flex;
          justify-content: center;
      }

      .manuscript-body {
          max-width: 650px;
          width: 100%;
          line-height: 1.75;
          font-size: 1.15rem;
      }

      .right-sidebar {
          width: 320px;
          border-left: 1px solid #e5e7eb;
          background: #ffffff;
          overflow-y: auto;
          padding: 24px 16px;
      }

      .right-sidebar h4 {
          font-size: 0.9rem;
          color: #374151;
          margin-bottom: 16px;
      }

      #manuscript-text {
          cursor: pointer;
          padding: 8px;
          border-radius: 4px;
          transition: background-color 0.2s ease;
      }

      #manuscript-text:hover {
          background-color: rgba(254, 240, 138, 0.2);
          /* Soft highlight hint on mouse hover */
      }

      #manuscript-text.active-highlight {
          background-color: #fef08a;
          /* Solid vibrant amber highlighter fill when clicked */
          border-left: 4px solid #eab308;
      }

      /* Add these rules straight into your HeadStyles CSS literal block string layout */
      .novel-sentence {
          cursor: pointer;
          padding: 2px 0;
          transition: background-color 0.15s ease;
          border-radius: 2px;
      }

      .novel-sentence:hover {
          background-color: rgba(254, 240, 138, 0.4);
          /* Light translucent highlighter pen glow on mouse over */
      }

      .novel-sentence.active-highlight {
          background-color: #fef08a;
          /* Vibrant fixed highlighter block fill when active */
          border-bottom: 2px solid #eab308;
      }